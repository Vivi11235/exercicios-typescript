"use strict";
//Aluno > identificador da classe
/*export class Aluno {
    nomeCompleto: string = "Wallace";
    CPF: string = "000.111.222-33";
    Idade: number = 25;
    habilidades: string[] = [];
    notes: number[] = [];
    turma: string = 'Programa Starter';
    idade: number;

    //construtor é a função padrão que sempre executa quando a gente instancia uma classe, serve para setarmos os valores para os atributos de classe.

    constructor(nomeAluno: string, CPFAluno: string, idade:number) {
        //parametro
        this.nomeCompleto = nomeAluno;
        this.CPF = CPFAluno;
        this.idade = idade;

        //default
        this.habilidades = [];
        this.notas = [];
    }

    aprenderHabilidade(novaHabilidade: string): void {
        //aqui é uma rotina que vai executar para modificar uma propriedade
        this.habilidades.push('CSS');
        this.habilidades.push('novaHabilidade');
    }

    realizarAtividade() {
        //aqui é uma rotina que vai executar para modificar uma propriedade
    }

    mudarTurma() {
        //aqui é uma rotina que vai executar para modificar uma propriedade
    }
}*/ 
